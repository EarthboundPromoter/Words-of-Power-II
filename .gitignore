# Local / generated â€” not for distribution
__pycache__/
*.pyc
*.log
logs/
telemetry/
telemetry.py
telemetry_enabled

# Internal dev docs (kept locally, out of the public repo)
docs/

# PyInstaller build artifacts
build/
dist/
*.spec

# The live config is per-install state, never a release artifact — the
# v0.3.1 dev-config shipping accident is why this line exists.
settings.ini
